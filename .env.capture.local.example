# Copy to .env.capture.local (gitignored via .env*.local) and fill for authenticated screenshots.
# Do not commit real passwords. Rotate if accidentally exposed.

AGIGX_UI_LOGIN_EMAIL=
AGIGX_UI_LOGIN_PASSWORD=
